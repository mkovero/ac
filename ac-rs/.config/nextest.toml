# cargo-nextest settings, used by bin/gate.sh.
#
# Nothing to configure: the ac-cli integration tests that spawn daemons used
# to need serial test groups here, because their per-binary port cursors
# collide across processes. They now take a cross-process lease per port pair
# (crates/ac-cli/tests/support/mod.rs).
